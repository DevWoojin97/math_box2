//type (a: number, b: number) -> number;
const add = (a, b) => a + b;

// type(a: number, b: number) -> number;
const substract = (a, b) => a - b;

// type(a: number, b: number) -> number;
const multiply = (a, b) => {
  return a * b;
};

// git init - 로컬 레포지토리 만들기
// git add . - stage로
// git commit - 로컬 레포지토리에 저장하기
// git push - 로컬 레포지토리와 리모트 레포지토리 동기화 하기
